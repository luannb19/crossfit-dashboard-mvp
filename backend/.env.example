# App
NODE_ENV=development
PORT=4000
JWT_SECRET=dev

# Credenciais de desenvolvimento (apenas local/testes)
DEMO_USER_EMAIL=gestor@insightflow.com
DEMO_USER_PASSWORD=123456
DEMO_USER_ID=u1
DEMO_USER_ROLE=GESTOR

# Banco (local: opcional; CI já usa Postgres)
# Para Postgres local: postgresql://user:password@localhost:5432/insightflow?schema=public
# Para SQLite local (se preferir): file:./dev.db
DATABASE_URL=
